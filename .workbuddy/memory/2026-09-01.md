# 2026-09-01

## Simple Frustration — 4-player mode (advanced feature)

Continues the work logged in `2026-08-31.md`.

- **Key fix:** `BoardConfiguration.homeOf(colour)` → `homeOf(colour, playerCount)`, because homes depend on player count:
  - Standard: 2p Red=1/Blue=10; 4p Red=1/Blue=5/Green=10/Yellow=14.
  - Large: 2p Red=1/Blue=19; 4p Red=1/Blue=10/Green=19/Yellow=28.
- Added `PlayerManager` (game pkg): ordered list, turn rotation (`current()`/`advance()`), `totalPlays()`, `findVictim()`. `Game` refactored to use it (removed its own findVictim/totalPlays/current).
- CLI `Main`: added `--players 2|4`; win-rate generalized via `Map<PlayerColor,Integer>`.
- Added `demo/FourPlayersDemo.java` reproducing the brief's 4-player worked example.
- Verified: 2p regression OK; brief 4p example (large+precise+hit, seq 12,5,2,12,7,4,2,12,3,5,2,12,5,4,4,8,2,2,12,3) reproduced byte-for-byte (Yellow wins 5 / total 20; Green + Red hit home); standard 4p homes correct; 4p win-rate (Red>Blue>Green>Yellow) works.

## Undo feature (Command + Memento + UndoManager)

- Added `game/TurnMemento` (immutable snapshot: all players' positions+plays + currentIndex), `game/TurnCommand` (interface: execute/undo), `game/Turn` (concrete command, two-phase `roll()` preview then `commit()` apply move+hit; captures memento in ctor, `undo()` restores).
- Added `game/UndoManager` (per-player credits, DEFAULT_MAX_UNDOS=3) + `game/UndoDecision` (@FunctionalInterface hook).
- `Game` refactored: `playTurn`/`resolveHit` moved into `Turn`; `run` loop now creates a `Turn` per turn, consults `UndoDecision` after roll, undoes via `UndoManager` (prints "X undoes! N undos remaining" / "has no undos left") before commit. New `playWith(UndoDecision)`; `play()` = `playWith(null)`; `simulate()` unchanged (no undo).
- `Player` + `restoreTo(Position, plays)`; `PlayerManager` + `currentIndex()`/`setCurrentIndex(int)`.
- Added `demo/UndoDemo` (2 scenarios): undo+re-roll (fixed 12,12,7,8) and 3-undo limit (fixed ten 5s).
- Verified: all 5 brief Demos, HIT, 4-player brief example still byte-identical; UndoDemo prints correct undo/limit flow (Red undo 2→1→0 remaining, then "no undos left" forced commit).

## README.md (final deliverable)

- Wrote `README.md` at project root: title/intro, project tree (38 files + base SimpleFrustration.java), class-responsibility table, 5 design patterns (Strategy/Singleton/SimpleFactory/Command/Memento + encapsulation/immutable/DI), 4 variations + advanced (4p/undo/winrate/CLI), execution flow (Mermaid flowchart + class diagram), how-to-run, 6 sample outputs (captured exact via running), limitations/improvements.
- Project totals: `frustration/` = model(5) board(5) dice(5) rule(6) game(9) demo(7) + Main = 38 files.
- Note: README written in Chinese (per user's Chinese request); offered English version for MMU submission.
- Also wrote `README_EN.md` (pure English, same 8 sections) for the MMU submission. Chinese `README.md` retained.

## Folder rename (blocked)

- User asked to rename project folder `计算机设计与架构` → English. Confirmed via AskUserQuestion: new name = `SimpleFrustration`.
- IntelliJ config uses relative `$PROJECT_DIR$` paths; `.iml` already `TASK1.iml` (English) → rename is safe (no stale references).
- RENAME FAILED: folder "in use" (Device or resource busy). Cause: WorkBuddy.exe holds it as the active workspace (cwd/handle). Must close WorkBuddy (or this workspace) and rename in File Explorer. Pending: user will rename externally, then I should verify compile/run still OK at new path.

## Main usability fix (discoverability)

- User worried the teacher won't know how to switch variations (esp. 4 players). Changed `Main`: no-args (or `--help`) now prints a full usage menu listing all flags + example commands (incl. `--players 4`, four-variation combo, `--games`, and a pointer to `demo.Demos`). Enriched `printUsage()` with an "Examples" block.
- Updated README.md + README_EN.md §6.3 first example: "no options -> prints help menu" (was "basic game (random)").
- Verified: `java Main` (no args) prints usage; `--players 4` runs 4-player game correctly.

## Main.java repair (user's IntelliJ errors)

- User reported IntelliJ build errors. Root cause: user had manually edited `Main.java` — removed `package frustration;`, changed `java frustration.Main` strings to `java Main`, and `Integer::sum` failed type-check under IntelliJ's openjdk-25 (resolves to IntBinaryOperator, not BiFunction).
- Fixed: restored `package frustration;`; reverted strings to `java frustration.Main`; changed `Integer::sum` → `(a, b) -> a + b`.
- Reverted no-args behavior back to "play basic game" (was "print help") — teacher expects to SEE a game on run; `--help` still prints usage. Verified compile + all modes.

## Main no-args behavior: final = "print help" (user's choice)

- User asked to roll Main back to "no args -> print help menu". Restored `if (a.help || args.length == 0)` while KEEPING the three bug fixes (package decl, `java frustration.Main` strings, `(a,b)->a+b`). Verified compile; no-args prints usage, `--players 4` runs game.
- Note for future: `Main.java` behavior decision has flip-flopped; current committed git HEAD = "完整版" has "no-args = game" + `Integer::sum` + simpler printUsage. User's uncommitted working tree now = help menu + bug fixes.

## Main no-args FINAL = "play basic game" (user reverted again)

- User decided to revert once more: no-args -> directly play basic game (not help). Changed `if (a.help || args.length == 0)` back to `if (a.help)`. Verified: no-args runs a random game, `--help` prints usage. Bug fixes (package/strings/lambda) still in place.

## IntelliJ errors: root cause was .iml + emptied TurnCommand.java

- User got IntelliJ errors again. Two causes:
  1. `TASK1.iml` had TWO source folders (project root AND `frustration/` subfolder) — IntelliJ/ user added the second one during earlier manual edits. Removed the extra line.
  2. `frustration/game/TurnCommand.java` had been emptied (1 empty line, no package declaration) — likely an accidental deletion. Recreated file with correct package + content.
- Verified: `javac` compiles all 38 files; `java frustration.Main` runs a game (Red wins 3 / total 5). No-args = game, --help = usage, --players 4 = 4-player.

## DISASTER RECOVERY: user's git merge wiped all uncommitted source

- User did a git merge/checkout/reset that reverted the working tree to commit `d7a818e 完整版`, which only contained `.gitignore`, `SimpleFrustration.java`, `frustration/Main.java` (OLD), `.iml`. All subpackage `.java` files were UNCOMMITTED → wiped from disk (frustration/ left with only Main.java).
- Recovery path: `out/production/TASK1/` still had 46 compiled `.class` files (latest state), AND all source was reconstructable from context.
- Rebuilt ALL 38 files: model(5) board(5) dice(5) rule(6) game(9) Main(1) demo(7). Demo dice sequences / board configs / player counts were recovered exactly by `javap -c -constants` on the stale `.class` files (FourPlayersDemo=20-roll brief seq; LargeBoardDemo=12x6+4,5 / 18x5 / 12,6,12,12,11,12,6; HitBackToHomeDemo=8,2,3,4,9 + 9,12,8,7,2,8,3; SingleDiceDemo=6,6,6,6,3,4,3,4; PreciseLandingDemo=T2+5/1/3/4).
- Verified: 38 files compile; basic (12,12,7,8) -> Blue wins 2/total 4; 4p brief -> Yellow wins 5/total 20; Demos all 5 match; UndoDemo undo/limit flow correct.
- IMPORTANT lesson for user: commit work regularly; the only commits were very early ("Initial commit" + "完整版"), so weeks of work lived only in the working tree. Remote `origin/master` (8b63999 "Add files via upload") is also just the early state. Advise: `git add -A && git commit` now.
