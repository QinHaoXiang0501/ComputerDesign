package frustration.board;

/**
 * The two board sizes the game supports.
 */
public enum BoardType {
    /** 18 main squares, 3 tail squares. */
    STANDARD,
    /** 36 main squares, 6 tail squares. */
    LARGE
}
